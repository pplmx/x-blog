# Search Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现博客搜索功能，支持实时搜索建议和独立搜索结果页面

**Architecture:** 后端添加搜索 API（SQLAlchemy 模糊搜索），前端添加搜索框组件和搜索结果页面

**Tech Stack:** FastAPI, SQLAlchemy, Next.js, React

---

## 文件结构

```text
backend/
├── app/
│   ├── crud.py           # 修改: 添加 search_posts 函数
│   ├── routers/
│   │   └── search.py     # 新增: 搜索 API 路由
│   └── main.py           # 修改: 注册搜索路由

frontend/
├── app/
│   ├── search/
│   │   └── page.tsx      # 新增: 搜索结果页
│   └── layout.tsx        # 修改: 添加搜索框
├── components/
│   ├── SearchBox.tsx     # 新增: 搜索框组件
│   └── SearchResults.tsx # 新增: 搜索结果列表
├── lib/
│   └── api.ts            # 修改: 添加 search 函数
└── types/
    └── index.ts          # 修改: 添加 SearchResult 类型
```

---

### Task 1: 后端 - 添加搜索 API

**Files:**

- Create: `backend/app/routers/search.py`
- Modify: `backend/app/crud.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 添加 search_posts 函数到 crud.py**

在 `backend/app/crud.py` 末尾添加：

```python
def search_posts(db: Session, query: str, page: int = 1, limit: int = 10):
    offset = (page - 1) * limit

    search_pattern = f"%{query}%"

    stmt = (
        select(Post)
        .where(
            or_(
                Post.title.ilike(search_pattern),
                Post.content.ilike(search_pattern),
            )
        )
        .where(Post.published == True)
        .order_by(
            Post.title.ilike(search_pattern).desc(),
            Post.created_at.desc()
        )
        .offset(offset)
        .limit(limit)
    )

    posts = db.execute(stmt).scalars().all()

    count_stmt = (
        select(func.count(Post.id))
        .where(
            or_(
                Post.title.ilike(search_pattern),
                Post.content.ilike(search_pattern),
            )
        )
        .where(Post.published == True)
    )
    total = db.execute(count_stmt).scalar()

    return posts, total
```

- [ ] **Step 2: 创建 search.py 路由**

创建 `backend/app/routers/search.py`：

```python
from fastapi import APIRouter, Query
from sqlalchemy.orm import Session
from typing import Optional

from ..database import get_db
from ..crud import search_posts
from ..schemas import PostList

router = APIRouter(prefix="/api/search", tags=["search"])

@router.get("")
def search(
    q: str = Query(..., min_length=1),
    page: int = Query(1, ge=1),
    limit: int = Query(10, ge=1, le=50),
    db: Session = Depends(get_db)
):
    posts, total = search_posts(db, query=q, page=page, limit=limit)

    return {
        "items": [PostList.model_validate(p) for p in posts],
        "pagination": {
            "page": page,
            "limit": limit,
            "total": total,
            "total_pages": (total + limit - 1) // limit
        }
    }
```

- [ ] **Step 3: 注册路由到 main.py**

在 `backend/app/main.py` 添加：

```python
from .routers import search

app.include_router(search.router)
```

- [ ] **Step 4: 测试搜索 API**

```bash
cd backend && uv run pytest tests/test_posts.py -v
```

- [ ] **Step 5: 提交**

```bash
git add backend/app/crud.py backend/app/routers/search.py backend/app/main.py
git commit -m "feat: add search API endpoint"
```

---

### Task 2: 前端 - 添加搜索相关类型和 API

**Files:**

- Modify: `frontend/types/index.ts`
- Modify: `frontend/lib/api.ts`

- [ ] **Step 1: 添加 SearchResult 类型**

在 `frontend/types/index.ts` 末尾添加：

```typescript
export interface SearchResult {
  items: PostList[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    total_pages: number;
  };
}
```

- [ ] **Step 2: 添加 search 函数到 api.ts**

在 `frontend/lib/api.ts` 末尾添加：

```typescript
export async function searchPosts(
  query: string,
  page: number = 1,
  limit: number = 10
): Promise<SearchResult> {
  const params = new URLSearchParams({
    q: query,
    page: page.toString(),
    limit: limit.toString(),
  });

  const res = await fetch(`/api/search?${params}`);
  if (!res.ok) {
    throw new Error("Search failed");
  }
  return res.json();
}
```

- [ ] **Step 3: 提交**

```bash
git add frontend/types/index.ts frontend/lib/api.ts
git commit -m "feat: add search types and API client"
```

---

### Task 3: 前端 - 创建搜索框组件

**Files:**

- Create: `frontend/components/SearchBox.tsx`

- [ ] **Step 1: 创建 SearchBox 组件**

创建 `frontend/components/SearchBox.tsx`：

```typescript
"use client";

import { useState, useRef, useEffect } from "react";
import { useRouter } from "next/navigation";
import { searchPosts } from "@/lib/api";
import { PostList, Category, Tag } from "@/types";

interface Suggestion {
  type: "post" | "category" | "tag";
  item: PostList | Category | Tag;
}

export default function SearchBox() {
  const [query, setQuery] = useState("");
  const [suggestions, setSuggestions] = useState<Suggestion[]>([]);
  const [show, setShow] = useState(false);
  const [loading, setLoading] = useState(false);
  const router = useRouter();
  const inputRef = useRef<HTMLInputElement>(null);
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const handleClickOutside = (e: MouseEvent) => {
      if (containerRef.current && !containerRef.current.contains(e.target as Node)) {
        setShow(false);
      }
    };
    document.addEventListener("mousedown", handleClickOutside);
    return () => document.removeEventListener("mousedown", handleClickOutside);
  }, []);

  useEffect(() => {
    if (!query.trim()) {
      setSuggestions([]);
      return;
    }

    const timer = setTimeout(async () => {
      setLoading(true);
      try {
        const result = await searchPosts(query, 1, 5);
        const posts = result.items.map((p) => ({ type: "post" as const, item: p }));
        setSuggestions(posts.slice(0, 5));
      } catch {
        setSuggestions([]);
      } finally {
        setLoading(false);
      }
    }, 300);

    return () => clearTimeout(timer);
  }, [query]);

  const handleSearch = (q: string) => {
    setShow(false);
    router.push(`/search?q=${encodeURIComponent(q)}`);
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === "Enter" && query.trim()) {
      handleSearch(query);
    }
  };

  return (
    <div ref={containerRef} className="relative">
      <input
        ref={inputRef}
        type="text"
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        onKeyDown={handleKeyDown}
        onFocus={() => query.trim() && setShow(true)}
        placeholder="搜索文章..."
        className="px-3 py-1.5 border rounded-md text-sm w-48 focus:outline-none focus:ring-2 focus:ring-blue-500"
      />

      {show && query.trim() && (
        <div className="absolute top-full mt-1 right-0 w-64 bg-white border rounded-md shadow-lg z-50">
          {loading ? (
            <div className="p-3 text-sm text-gray-500">加载中...</div>
          ) : suggestions.length > 0 ? (
            <ul>
              {suggestions.map((s, i) => (
                <li key={i}>
                  <button
                    onClick={() => handleSearch(s.item.name || (s.item as PostList).title)}
                    className="w-full text-left px-3 py-2 text-sm hover:bg-gray-100"
                  >
                    {s.type === "post" ? (s.item as PostList).title : s.item.name}
                  </button>
                </li>
              ))}
            </ul>
          ) : (
            <div className="p-3 text-sm text-gray-500">无结果</div>
          )}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/components/SearchBox.tsx
git commit -m "feat: add SearchBox component with real-time suggestions"
```

---

### Task 4: 前端 - 添加搜索结果页面

**Files:**

- Create: `frontend/app/search/page.tsx`
- Create: `frontend/components/SearchResults.tsx`

- [ ] **Step 1: 创建 SearchResults 组件**

创建 `frontend/components/SearchResults.tsx`：

```typescript
import { PostList } from "@/types";
import PostCard from "@/components/PostCard";

interface SearchResultsProps {
  posts: PostList[];
  query: string;
}

export default function SearchResults({ posts, query }: SearchResultsProps) {
  if (posts.length === 0) {
    return (
      <div className="text-center py-12">
        <p className="text-gray-500">未找到与 "{query}" 相关的文章</p>
      </div>
    );
  }

  return (
    <div>
      <p className="mb-6 text-gray-600">找到 {posts.length} 个结果</p>
      <div className="grid gap-6">
        {posts.map((post) => (
          <PostCard key={post.id} post={post} />
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建搜索结果页面**

创建 `frontend/app/search/page.tsx`：

```typescript
import { searchPosts } from "@/lib/api";
import SearchResults from "@/components/SearchResults";
import Pagination from "@/components/Pagination";
import { notFound } from "next/navigation";

export const dynamic = "force-static";
export const revalidate = 60;

interface SearchPageProps {
  searchParams: Promise<{ q?: string; page?: string }>;
}

export async function generateMetadata({ searchParams }: SearchPageProps) {
  const params = await searchParams;
  const query = params.q || "";

  return {
    title: query ? `搜索: ${query}` : "搜索",
    description: `搜索文章: ${query}`,
  };
}

export default async function SearchPage({ searchParams }: SearchPageProps) {
  const params = await searchParams;
  const query = params.q || "";
  const page = params.page ? parseInt(params.page) : 1;

  if (!query) {
    return (
      <div className="text-center py-12">
        <p className="text-gray-500">请输入搜索关键词</p>
      </div>
    );
  }

  const result = await searchPosts(query, page, 10);

  return (
    <div>
      <h1 className="text-3xl font-bold mb-8">搜索结果</h1>
      <SearchResults posts={result.items} query={query} />
      <Pagination
        currentPage={page}
        totalPages={result.pagination.total_pages}
        baseUrl={`/search?q=${encodeURIComponent(query)}`}
      />
    </div>
  );
}
```

- [ ] **Step 3: 将搜索框添加到 Header**

修改 `frontend/components/Header.tsx`：

```typescript
import SearchBox from "./SearchBox";

// 在导航栏中添加 SearchBox
// <div className="flex items-center gap-4">
//   <SearchBox />
//   ...
// </div>
```

- [ ] **Step 4: 提交**

```bash
git add frontend/app/search/page.tsx frontend/components/SearchResults.tsx frontend/components/Header.tsx
git commit -m "feat: add search results page and integrate SearchBox"
```

---

## 验证

完成所有任务后：

1. 后端测试通过
2. 前端构建通过

```bash
cd /var/tmp/aurora && just test
cd frontend && pnpm build
```

(End of plan - 12 steps)
