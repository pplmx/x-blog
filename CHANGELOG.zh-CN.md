# 更新日志

本项目所有重要变更都会记录在该文件中。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

[English](./CHANGELOG.md) · [中文](./CHANGELOG.zh-CN.md)

## [Unreleased]

- 🛠️ **文章页阅读打磨（round 411）**：面向读者的文章页现在会正确清理其目录观察器。
  **离开文章后，移动端目录的标题扫描不再被触发**：`setupTocObserver` 用一个从未被记录的 500 ms
  沉降定时器推迟标题扫描——在该窗口内通过 SPA 跳到上一篇/下一篇（或直接关闭标签页），这个"迟到的"
  定时器会对*下一篇文章*的 DOM 执行扫描（重复观察它的标题），或对着已销毁的文档执行扫描。
  现在每次重建时与卸载时都会取消待处理的扫描（与点赞失败、续读条定时器相同的卫生习惯），
  且一旦观察器已断开，扫描本身也会跳过。前端 2454 个单元测试（round 411, TASK-489, ISS-564）。
- 🛠️ **细节与读者认证打磨轮（rounds 403–409）**：六轮以验证为先的打磨横跨前后端，同时清空了
  读者认证审查的全部积压。**无效的深链不再死循环**：讨论、搜索与读者主页把 `?page=0` /
  `?page=abc` 钳到第 1 页，而不是把请求发给后端被 422 拒绝（再在同样的失败里反复重载）。
  **更新文章会校验引用**：在管理编辑表单里填写一个已不存在的作者或标签，现在会快速失败并给出
  清晰报错，而不是静默丢弃该关联（编辑接口与底层 CRUD 两条路径都覆盖）。**分页与格式化跟随
  站点语言**：关注流的当前页高亮改为读取 URL 而非滞后的服务端负载，管理端媒体/文章表格与首页
  统计按站点语言（zh-CN / en-US）渲染日期与数字，而不是按浏览器语言。后端：**批量删除评论不再
  跑约 300 条查询**——孤儿修复（子评论上移、回查父级、清理标记/点赞）批量为三条批量语句；
  **给评论点赞是原子的**——喜欢行与计数递增在同一事务里落库，崩溃不再留下「有点赞却没计数」
  的不一致；以及**访客话题关注的重启同意闸门**（与 round 393 的 newsletter 对等）：退订后重放
  原始双重确认链接不再能悄悄重新订阅——confirm 现在 400 进入「重新订阅」状态，且各确认页把有意的
  400 / 未知 token 404 / 无法连通区分成各自诚实的文案（网络抖动不再被误报为「无效链接」，退订
  地址也不再被要求重试已失效的链接）。**重置密码在跳转 /account 被打断时也展示成功状态**——
  已登录的读者绝不会落在空白页。**登录后回到你的出发点**：喜欢、关注与通知页现在把
  `?redirect=/<route>` 带给 /login，读者点过登录提示后会回到触发登录的那一页，而不是 /bookmarks
  （登录页的开放跳转守卫只接受站内相对路径，新值天然安全）。**版权页脚改为渲染当前年份**，而不是
  写死的 2026。Backend 1898 @ 97.26%，frontend 2452 单测，typecheck/lint/PG 迁移全绿
  （rounds 403–409）。
  四项修复。**评论提交不再把 ofetch 的技术性错误串**（含内部 URL 的 `"[POST] …: 429 Too Many
  Requests"`）泄露到表单的红色告警里——**429 和评论已关闭的 403 现在显示本地化文案**，其它
  拒绝则展示后端的可读信封信息并带干净回退。**/liked 页取消喜欢现在诚实**：该页是**服务端**的
  喜欢集合，当云端 DELETE 落空（离线/5xx）时卡片保留并给出重试提示，而不是出现一张会在下次
  访问时悄悄 "复活" 的卡片——`unlike()` 会把本地标记回滚并如实报告删除是否持久化。**分页高亮
  跟随你点击的页码**：首页、归档、分类与讨论页现在从 URL/点击目标推导当前页标记，而不是读取
  在途的旧服务端载荷——高亮按钮不再 "地址栏显示 3、按钮却灰着 1"。后端侧：**评论审核通知只触发
  一次**——审核员"驳回后再重新通过"不再把整套多通道通知重发一遍（重复推送、重复站内收件箱行、
  重复邮件给被回复者、@提及者、关注者与每位游客订阅者）；`Comment.notified_at` 像
  `Post.new_post_notified_at` 一样在首次通过时打戳。**游客周报不再把订阅者自己的评论回读给
  他**（逐评论通道本就跳过本人）。**新闻通讯取消是实打实的**：取消订阅后重放旧的确认链接无法再
  悄悄把地址重新激活——重新订阅必须走全新订阅 + 全新双重确认邮件（`unsubscribed_at`）；并且
  **邮箱更改现在先持久化再发送**，临时数据库故障不会寄出一封无法兑现的验证链接。账号页的
  **「当前密码错误」的 401 不再被误判为会话过期**——会话过期检测现在读取后端的
  `{"error":{"message"}}` 信封，修改密码/删除账号时输错当前密码会显示表单错误，而不是被悄悄
  登出。后端 1879 + 10 跳过、覆盖率 93.7%，前端 2405 个单元测试，类型检查/lint/迁移在
  PostgreSQL 上全绿（round 393, DEC-448）。
- 🛠️ **读者认证打磨轮（round 392）**：对登录链路做了一次深挖。认证表单此前显示的是 ofetch 的
  **技术性错误串**（`"[POST] ...: 401 Unauthorized"`）而不是后端的人类可读信息——现在密码错误、
  2FA 验证码错误、注册被拒都会显示真实原因（「邮箱或密码不正确」「该邮箱已被注册」等），从
  `{"error":{"message":…}}` 信封中读取并带完整回退（登录、2FA、注册、重置密码）。会话存储也做了
  **防 localStorage 写失败加固**：隐私模式配额或 Safari 第三方存储拦截不再让一个服务端已成功的
  认证请求报错——内存会话仍承载当前标签页。**登录跳转不再可被利用**：`?redirect=/\evil.com`
  此前能通过旧的同源判断，但 WHATWG URL 规范化会把反斜杠折叠成 `//`，变成跨域跳转——凡含
  反斜杠的跳转值现在一律回退到 /bookmarks。**注册的昵称输入框**带上后端 50 字符上限的客户端
  `maxlength`，超长昵称立即失败而不是提交后才报 422。**「← 返回登录」链接现在真的去 /login**——
  携带 token 时它此前被误标成跳往 /forgot-password。前端 2399 个单元测试（+10）、类型检查与
  lint 全绿（round 392, DEC-446/447）。
- 🛠️ **Bug 修复与打磨轮（round 391）**：全栈修复五个已核实缺陷。**账号删除现在真正彻底**：
  `delete_reader_account` 此前会残留文章点赞、作者关注以及读者间互关/拉黑的“双方”数据，
  在 SQLite（无 AUTOINCREMENT）下未来账号可能继承被回收的 id 而串入他人数据或被嫁接关系——
  这些行现在一并清理。**回复通知深链落在新回复上**，而不是你自己的父评论（DEC-072 承诺的
  “点开直达这条回复”此前锚错了位置）。**上传存储目录**改用与媒体库相同的 naive-UTC 时钟，
  非 UTC 时区服务器不再可能把文件写进比其展示时间戳早一个月/年的目录。**搜索页**上，评论模式
  零命中不再误导地提示“调整筛选条件”并给出一个无效的“清除筛选”按钮（搜索模式被误当成过滤
  条件）；**「你是不是想找」建议在“把一个拼错改成另一个拼错”时会刷新**，而不是停留在旧词条上。
  同一轮的**管理后台打磨**又修复了四个：**「通知订阅者」不再广播未保存的表单**（先经自动保存
  落库，未成功则提示“请先保存再通知”——此前未保存的 slug 修改会给每个订阅者推一条 404 深链），
  文章编辑器的**「取消」现在会先确认**再丢弃未保存修改（它之前就静悄悄待在「保存」旁边），
  三个裸 fetch 管理流程（图片上传、修改密码、审核提醒推送开关）**过期会话现在会跳回登录页**而不是把操作者困在
  一条通用报错上，仪表盘的**待审核评论计数在审核/驳回后保持同步**（大数字卡此前与快捷卡互相
  矛盾，快捷卡清空后还会在服务端仍有积压时显示为空）。后端 1873 + 10 跳过、前端 2389 个
  单元测试、类型检查与 lint 全绿（round 391, DEC-444/445）。
- 💡 **搜索建议「你是不是想找」（round 390）**：文章搜索是精确子串 + tsvector、没有模糊
  层，一个拼错或记不清的查询（recatvie、或错一个字的「异步编程实贱」）此前会死在空结果
  页。零命中的文章搜索现在会调用新增的 `GET /api/search/suggest?q=`，该端点用纯 Python
  编辑距离对有限词汇表评分——标签/分类名称 + 近期公开文章标题——因此中文一字之差与英文
  拼错完全同权（汉字按码点计距），也不依赖任何 Postgres 扩展。空结果显示「你是不是想
  找：」建议标签，点击即以修正后的词重新搜索（保留已有分类/标签/日期筛选）。后端不会推
  荐零命中话题、词汇表有界、且与搜索共用限流桶——客户端只在搜索确实零命中时才发起请求。
  后端 1873 + 前端 2386 个单元测试 + Playwright 全流程（发布 → 拼错搜索 → 建议标签 →
  修正后结果）全绿（round 390, DEC-443）。
- 🔎 **在评论线程内搜索（round 389）**：一条长讨论（支持最新/最早/最有帮助排序、任意
  分页）此前无法找到「那条提到 X 的回答」——全局评论搜索无法限定到单篇文章。线程现在
  在排序控件旁增加一个防抖搜索框：输入关键词后，列表在后端收窄为内容包含所有词的文章
  评论（`GET /api/comments/post/{id}?q=`，大小写不敏感的子串 AND，与全局评论搜索相同
  的语言无关路径，因此中文也能按片段匹配）。关键词会重置回第 1 页、可与任意排序组合，
  清除按钮恢复完整线程，LIKE 通配符被转义——输入 `%` 只匹配字面量。后端 1846 + 前端
  2382 单测 + Playwright 全旅程（种入带标记的线程 → 搜索框收窄 → 清除恢复）全绿
  （round 389, DEC-442）。
- 📝 **行内 Markdown 脚注（round 388）**：作者引用来源时此前无法渲染脚注——GFM 的
  `[^1]` / `[^1]: ...` 标记会在正文中逐字输出为原始文本。前端 marked 集成（帖子正文、
  打印/PDF 路由、以及 admin 编辑器预览共用的 markdown 渲染管线）现在内置一个零依赖的
  脚注扩展：正文中的标记变为可回跳的 `<sup>` 上标，定义行变为紧凑的引用列表（每项带
  id 与「↩」回链），脚注内的粗体、链接、行内代码照常生效。后端全量 RSS/Atom 订阅源
  同步修复（启用 python-markdown 的 `footnotes` 扩展，并为回链锚点放行 `id`/`class`
  属性）。一处渲染改动治愈所有消费面。后端 1844 + 前端 2378 单测 + Playwright 全旅程
  （发布 → 引文正确渲染 → 无裸 `[^1]` 泄漏）全绿（round 388, DEC-441）。
- 🔥 **本周热门（round 387）**：首页「热门文章」按累计阅读量排序，一篇刚发布却在被迅速
  阅读的新文章很难露脸。首页现在新增「本周热门」榜，依据当日分表 `post_views_daily`
  （DEC-086，每次访问都会原子累加）的窗口内阅读量排名——每张卡片展示「本周 N 次阅读」，
  与累计阅读数区分开。新增公开接口 `GET /api/posts/trending/list?days=7&limit=5`（仅返回
  已发布且已生效的文章；窗口为空时返回 `[]`，该区块自动隐藏——新装实例的分表只向前记录）。
  后端 1843 + 前端 2371 个单测 + Playwright 全旅程（发布新文章 → 刷阅读 → 出现在榜单）
  全绿（round 387, DEC-438）。
- 👤 **隐藏已屏蔽读者的评论（round 386）**：round 379 上线的读者屏蔽（DEC-425）虽然抑制了
  通知推送，但其设计初衷承诺“被屏蔽的评论者即使在线程中对你也是不可见的”——可惜没有任何
  渲染路径兑现这一点：被屏蔽读者的评论依旧出现在文章评论线程、讨论流和评论搜索里。屏蔽是
  接收方侧的主动选择，所以现在抑制发生在屏蔽者自己的设备上：评论线程（CommentList）、
  `/discussion` 讨论流和评论搜索各自加载一次当前读者的 `/me/blocks` 屏蔽列表，并在渲染时
  丢弃被屏蔽读者发布的行；未屏蔽第三方回复通过既有的“父缺失提升”逻辑保留（藏起的是被屏蔽
  作者，绝不审查其受众）。匿名访客与屏蔽列表为空的读者看到的页面与之前完全一致，分页总数
  也始终是服务器的真实计数（屏蔽只是个人视角的过滤，不是全局删除）。Playwright 全旅程覆盖
  三个面——评论线程 → 讨论流 → 评论搜索。前端 2367 个单测 + e2e 旅程全绿（round 386,
  DEC-436/DEC-437）。
- ✏️ **访客评论管理（round 385）**：评论的编辑/删除此前只面向登录读者——匿名评论者打错
  字后没有任何办法修改或撤回（而每条评论都要过审，一旦通过就永久公开）。现在留下邮箱的
  访客会在评论过审时收到一封“你的评论已发布 — 可管理”邮件，携带与回复邮件相同的每条评论
  专属密钥（DEC-332）；邮件里的链接会打开独立的 `/comment-manage` 页面，访客可以在上面
  编辑评论文字（编辑会重新进入审核队列，与登录读者的编辑完全一致）或删除整条评论（其回复
  会被重新挂到上级，线程不受损）。新增按密钥鉴权的 GET/PATCH/DELETE
  `/api/comments/manage` 接口，完全镜像读者所有权语义；未知密钥一律 404，不可枚举。
  零 DDL——直接复用已有的密钥列与投递通道。后端 1839 + 前端 2358 个单测 + Playwright
  全旅程（留言并同意 → 过审 → 邮件链接 → 编辑 → 删除 → 链接失效）全绿（round 385,
  DEC-435）。
- 📴 **离线阅读（round 384）**：此前 Service Worker 只为 Web Push 存在，而且只有订阅推送
  的读者才会注册——其他访客完全没有离线能力。现在生产构建中 `/sw.js` 全站注册，并维护一个
  网络优先、有上限、仅作离线兜底的同源 GET 运行时缓存：访问过的文章会缓存其 HTML 文档、
  JS/CSS 分包与图片，断网后重载仍能正常渲染。在线时每个请求照常走网络（缓存只兜底、绝不
  提供过期内容），缓存超过 60 条自动淘汰最早的，API/管理端/订阅源与跨域请求一律不缓存。
  2351 个前端单测 + 离线阅读 e2e（注册 → 访问文章 → 断开网络 → 重载 → 文章仍渲染）全绿
  （round 384）。
- 🐎 **阅读统计聚合收敛（round 383）**：`/me/history/stats` 此前会扫遍读者的全部历史并
  物化每一行 Post——包括 `content`——在 Python 里逐行计算阅读分钟（重度读者每次打开
  /history 都要传输数 MB）。新增反规范化字段 `reading_history.reading_minutes`（由记录/
  导入写入路径维护、存量行回填以免总量跳变），让统计改为纯 SQL 聚合：COUNT/SUM/MAX +
  只取窄的 viewed_at 时间戳用于连续天数热力图；该端点从此不再把任何文章正文带出数据库。
  估算值按“最后一次阅读时的内容”计算，之后编辑文章不再改变读者的阅读总量。迁移已在
  SQLite 与 PostgreSQL 18 上验证（round 383, ISS-451）。
- 🕳️ **naive-UTC 硬化（round 383）**：`comments.reviewed_at/edited_at` 与
  `reading_history.viewed_at` 都是 naive-UTC 的 DateTime 列，但此前有 4 处写入（crud 的
  审核/编辑/记录/导入 + 管理端批量审核时间戳 + viewed_at 的 ORM 默认值）写入的是带时区
  的 `datetime.now(UTC)`——一旦行被缓存或序列化就会踩坑。现在统一改用 `utc_now_naive`
  助手（round 383, ISS-452）。
- 🧭 **顶栏收敛 + 「我的」头像菜单（round 382）**：登录后的顶栏已膨胀到 14 个平铺
  链接，加上搜索、推送、语言、主题——英文下需要 1400px+（xl 用隐藏滚动条硬撑），
  且登录前后宽度跳变。现在所有读者个人链接（收藏、阅读历史、我的评论、喜欢、关注、
  通知、账号）都收进一个「我的」头像下拉：迷你资料头（→ /account）、公开主页链接
  （→ /readers/{id}）、未读徽标直接挂到头像上，以及退出登录——完整复用语言切换器
  的键盘与点击外部关闭约定。登录只是把「登录」按钮换成头像，顶栏宽度恒定不再跳变；
  移动端面板则以不折叠的「我的」区块平铺同样的链接。旧的「authOnly 链接必须追加在
  末尾（SSR 节点复用）」约束随之坍缩为单个尾节点头像，「/search」平铺链接也被移除
  （HeaderSearch 常驻）。2347 个前端单测、typecheck 与 lint 全绿；my-comments e2e
  改为经由头像菜单进入（history 为游客旅程，直接跳转）。
- 🚫 **屏蔽读者（round 379）**：博客此前有读者互相关注、公开主页与 @提及通知，
  却没有任何方式让读者拦住某位烦人评论者的提及、回复与动态。读者在对方的公开
  主页即可一键 **屏蔽/取消屏蔽**（单向且不可见——对方不会收到任何提示、也能
  继续评论），后端提供 GET/PUT/DELETE `/api/reader/me/blocks`，且每个派发点
  （@提及、回复、线程评论广播、关注动态）都会在落库前剔除「已屏蔽该评论者的
  接收者」；`/account` 列出已屏蔽读者，一键取消屏蔽即可完整恢复。后端 1799
  测试 @93.77%（+18，已用 Postgres 验证）、前端 2318 测试（+9）、屏蔽旅程
  e2e 全绿（DEC-425, TASK-437）。
- 📧 **访客邮箱订阅讨论（round 380）**：评论讨论的订阅（DEC-078）此前只对
  登录读者开放，想用邮箱只订阅某一篇文章讨论的访客除了注册别无他路。文章
  评论头部新增一个紧凑订阅表单：访客输入邮箱、双重确认（免登录 202、对外
  统一回复、每条新记录只发一封确认邮件，同订阅模式）后，每有一条评论通过
  审核就收到一封讨论邮件——深链到该评论、自带每条订阅专属的一次性退订链接；
  待确认/已退订的记录保持静默，评论者本人的访客邮箱也会跳过。全程无需账号。
  后端 1814 测试 @93.72%（+15，已用 Postgres 验证）、前端 2332 测试（+14）、
  访客订阅讨论 e2e 旅程全绿（DEC-427, TASK-438）。
- 📧 **讨论周报选项（round 381）**：round 380 的访客订阅讨论是「每条通过的评论
  发一封邮件」——同时跟几篇活跃讨论的访客每天都会被刷屏，且毫无节奏可选，正是
  DEC-355 已替订阅堵上的缺口。已确认的访客现在可以选「每周汇总一次」（订阅时
  传入、确认页勾选，或 token 网关 `/guest/digest` 开关）：选择每周的订阅不再
  收到逐条评论邮件（每位订阅者只走一个通道），每周摘要任务（与订阅/读者周报
  同一 CLI/管理员触发）会按每篇被跟的讨论发一封汇总邮件，聚合当周通过审核的
  新评论——深链到每条评论、同样带一键退订。后端 1823 测试 @93.67%（+9，已用
  Postgres 验证）、前端 2335 测试（+3）、访客订阅讨论 e2e 旅程（两条）全绿
  （DEC-429, TASK-439）。
- 📧 **周报运维面板（round 378）**：每周摘要（读者 + 访客）此前只有完整的后端机器、
  却没有管理界面——管理员能用 CLI 或超管端点触发，却看不到谁订阅了周报、上次何时
  发出、下一期窗口将包含多少篇文章。`/admin/newsletter` 新增的周报面板
  （`GET /api/admin/newsletter/digest/overview`：活跃读者与已确认访客的周报订阅数、
  以 `digest_sent_at` 最大值为上次发送时间、滚动窗口内的公开文章数）补齐了这套阅读
  面，并提供 dry-run **预览**（`send-weekly?dry_run=true`，只报摘要、不发送不盖戳）
  与超管确认的 **立即发送**——让周报功能形成完整闭环。后端 1781 测试 @93.75%
  （+7，已用 Postgres 验证）、前端 2309 测试（+5）、周报面板 e2e 旅程全绿
  （DEC-423, TASK-436）。
- 📊 **`/history` 阅读洞察（round 372）**：连续阅读与 52 周热力图（DEC-316）展示的是
  阅读的**日历形状**——但想知道「这个月读了多少篇」或「我到底最常读哪个主题」的
  读者，除了原始行列表外没有任何聚合可看。新的 `GET /api/reader/me/history/insights`
  返回**内容形状**：全部与近 30 天内读过的（公开可见）去重文章数，加上常读分类
  （按去重文章数取前 5；每个读者-文章一行，同一篇反复读只算一次）。`/history`
  页在热力图旁渲染一个紧凑的洞察面板——「最近 30 天读过」卡片 + 常读分类标签——
  只对已登录读者展示，随列表并行、尽力而为地拉取（访客与任何失败场景下隐藏，
  从不阻塞列表）。沿用每条历史读取路径相同的公开可见性护栏：未发布/定时文章
  既不泄露也不计数。1774 个后端测试 @93.74%（+7）、2304 个前端测试与 1 条
  reading-insights e2e 全绿。
- 💔 **`/liked` 就地取消喜欢（round 371）**：每个读者自有的列表面都有管理入口，
  唯独「喜欢的文章」页没有——收藏页有逐行移除和「待读/已读」切换，而想收回一个赞
  还得回到文章页再点一次爱心。现在 `/liked` 每张卡片都带一个管理按钮：心形斜线
  的取消按钮，通过已有的幂等 `DELETE /api/reader/me/likes/{id}` 就地取消赞
  （公开计数同步递减），卡片与其计数立即消失（每行单飞、失败时恢复并显示错误
  提示），同时清掉本地喜欢标记——下次回文章页时爱心就是未点赞状态。卡片被重构
  成按钮与链接互为兄弟节点（避免 anchor 里嵌交互元素），删除最后一页最后一张时
  还会排水回载。纯前端改动：后端端点早已存在。2300 个前端测试与 1 条
  liked-unlike e2e 全绿。
- 🔍 **喜欢的文章回忆式搜索（round 370）**：最后一个还没有关键字搜索的读者自有
  内容面终于补齐——收藏页（DEC-124）、`/history`（DEC-148）与我的评论
  （round 369）都能按词筛选，唯独 `/liked` 页（round 359）只有分页；喜欢了很多
  文章的读者，找不到那篇现在才想起来的那篇。「/liked」新增带防抖（300ms）的
  搜索框，`GET /api/reader/me/likes` 新增可选 `q` 参数：对帖子标题/摘要做
  不区分大小写的匹配（LIKE，已转义），与公开可见性护栏和分页叠加。新的搜索
  从第 1 页重新开始；无匹配的搜索词显示「没有匹配的文章」空状态与一键
  「清除搜索」重置。过滤在 SQL 内执行，因此搜索覆盖全部喜欢的文章而非仅当前
  网格。覆盖率 93.72%，1767 个后端测试、2296 个前端测试与 1 条新的
  liked-search e2e 全绿。
- 🔍 **我的评论关键字搜索（round 369）**：评论历史很长的读者，现在可以按正文内容
  找到想找的那一条——这是最后一个还没有回忆式搜索的读者自有内容面（收藏页与
  `/history` 分别在 DEC-124 / DEC-148 就有了）。「我的评论」页新增带防抖（300ms）
  的搜索框，`GET /api/reader/me/comments` 也新增可选 `q` 参数：对评论正文做
  不区分大小写的匹配（LIKE，已转义，`%`/`_` 按字面匹配），可与状态筛选叠加，
  且始终限定在调用者自己的评论内。新的搜索从第 1 页重新开始；无匹配的搜索词
  显示「没有匹配的评论」空状态与一键「清除搜索」重置，而不是误导性的「还没有
  发表过评论」。过滤在 SQL 内执行，因此搜索覆盖全部历史而非仅当前加载页。
  覆盖率 93.72%，1757 个后端测试、28 个前端页面测试与 1 条 my-comments-search
  e2e 全绿。
- **讨论 RSS/Atom（round 368）**：讨论既能找（round 366）、也能逛（round 367），
  现在还能订阅——此前 RSS/Atom 只覆盖帖子（全站/分类/标签/作者/系列），对话流
  无法放进阅读器。新的 `/rss/comments.xml`（RSS 2.0）与 `/rss/comments.atom.xml`
  （Atom）流式推送全站最新已审核评论：每条条目携带评论者（显示名/笔名）+ 帖子
  标题，并深链到具体那条评论（`#comment-{id}`，DEC-321），而不是只到帖文标题。
  可见性护栏与讨论流/评论搜索完全一致——待审/被拒评论、草稿或未到发布时间帖子上
  的评论永不出现在 feed 里，内容永远不携带邮箱/IP；feed 按作用域键缓存、带 ETag
  支持条件请求（与帖子 feed 同一契约）。`/discussion` 页现在有「订阅讨论 RSS」
  链接与 RSS/Atom 自动发现标签，Nuxt 源站也有对应的专用代理路由。覆盖率 93.72%，
  1748 个后端测试、7 个前端测试与 2 条订阅 e2e 全绿。
- **最新讨论流（round 367）**：round 366 让讨论「可搜索」，但一位想看看站点此刻
  正在聊什么的访客仍然没有入口——讨论是博客的第二类内容资产，除逐帖评论区与搜索框
  之外，它没有一个「正在发生什么」的浏览面。新的公开页 `/discussion`（页脚也有
  静态链接）把全站最新已审核评论按时间倒序排成一条流：每张卡片带评论者身份（已登录
  评论者的公开显示名/头像）、评论正文与所在帖子的标题，点击即深链跳到
  `#comment-{id}`（DEC-321）那条评论上，而不是只到帖文标题。可见性护栏与评论搜索
  完全一致：待审/被拒评论、草稿或未到发布时间帖子上的评论永远不出现，邮箱/IP
  永远不离开后端；公开、无需登录、分页、带 SEO。覆盖率 93.71%，1741 个后端测试、
  13 个前端测试与 2 条讨论流 e2e 全绿。
- **公开评论搜索（round 366）**：搜索框过去只能搜文章标题/正文，讨论——博客的
  第二种内容资产——一直是不可搜索的。`/search` 现在新增「评论」模式（搜索范围
  tablist，URL 里以 `?type=comments` 存在，因此一条评论搜索分享链接会直接落进该
  模式）：按内容匹配已通过审核的评论（所有关键词都要命中），最新在前，每条结果带
  高亮片段（`<mark>` 前先转义，XSS 安全）+ 帖子简介，卡片深链到「该评论」
  （`#comment-{id}`，DEC-321）而不只是帖文标题。可见性护栏与文章搜索一致：待审/
  被拒评论、草稿或未到发布时间帖子上的评论永不命中；邮箱/IP 永远不会出现在结果里。
  仅文章专有的筛选（分类/标签/排序/日期范围）在评论模式下隐藏，翻页与换词都会保留
  当前模式；`%`/`_` 等 LIKE 元字符按字面匹配（review 反馈）。覆盖率 93.69%，
  1734 个后端测试、58 个前端搜索测试与 2 条评论搜索 e2e 全绿。
- **读者互相关注（round 365）**：关注轮盘——分类、系列、标签、作者都早已有「这
  个说话了就告诉我」的方向——终于补上「人对人」这一格：已登录读者可以在公开主页
  `/readers/{id}` 的顶部关注「另一位读者」（公开主页背后的评论者，即「关注作者」
  的读者版）。每位访客都能看到公开的粉丝数（同作者关注数）；已登录且非本人的读者
  会得到一个「关注 / 已关注」开关，订阅这位读者通过审核的评论。每当管理员审核通过
  一条你关注对象的评论，`/notifications` 下就会落一条带深链的 `reader_comment` 站内
  通知——回复仍会通知被回复的读者，且「同时关注了评论者」的父评论作者绝不会因同一
  条评论收到重复推送。已停用的关注者与关闭了该通知类型的读者都会被跳过，通知写入
  失败也绝不会破坏审核通过流程。`/account` 新增「关注的读者」区，可一键取消关注
  （取消后即停止推送）；`reader_comment` 是可关闭的按类型开关（DEC-171），与其
  他站内类型一致；新的粉丝数 + 调用者本人的关注状态随公开主页数据返回（无新接口、
  无 PII）。禁止关注自己；未知/已停用目标统一 404——不泄露「能否被关注」。新增
  `reader_follows` 表（每对唯一，`notify` 预留给以后的逐条关注控制）+ 一列
  `reader_notification_prefs.reader_comment`（迁移 l5n7p9r1t3v5、m7d9f1a3b5c7，
  SQLite 与 Postgres 均可往返）。
- **读者两步验证（round 364）**：round 358–363 以来读者账号积累了持久的私密数据——
  云端同步的收藏/点赞/阅读历史、GDPR 导出包、邮箱、关注、通知偏好与推送设备——
  全部只靠一个「邮箱 + 密码」凭据保护，因此一旦密码泄露，整个表面都暴露。现在读者
  可在 `/account` 把它提升到「密码 + TOTP 验证码」（RFC 6238，支持任意身份验证器
  App）：配置时返回 base32 密钥与 `otpauth://` 配置链接（前端渲染为二维码），开启
  两步验证需要当前密码 + 一个有效验证码——单独凭被窃取的会话无法注册一个主人无法
  移除的因子（安全评审 MEDIUM）。开启后 `POST /api/reader/login` 不再返回访问令牌，
  只返回短寿命、单一用途的 `mfa_token`（专属 `aud=x-blog-reader-2fa` 受众，5 分钟
  过期，绑定 `token_version`，改密即失效）；`POST /api/reader/login/2fa` 凭
  验证码 + 令牌换取真正的会话。第二步的任何失败都是同一个无法区分的 401（不会暴露
  「我的挑战令牌是否还有效」vs「验证码是否错误」）；关闭同样需要当前密码 + 有效验证码，
  并清除密钥（需重新配置）。新增 `two_factor_enabled`（`sa.false()`）与可空的
  `two_factor_secret` 两列（迁移 j3l7o9q1s5t3，SQLite + Postgres 双引擎往返验证）；
  密钥绝不进入任何资料或令牌响应。TOTP 后端使用新增的 `pyotp` 依赖。
- **公开「收藏的文章」主页标签（round 363）**：round 360 给了读者一个可选的
  「喜欢的文章」发现标签页；与之配套的另一条策展维度是读者选择「保留」（待读 /
  已读队列）而非仅仅「欣赏」的内容。`/account` 新增第二个勾选框（默认关闭，
  紫红色系，与点赞开关相互独立），开启后在公开 `/readers/{id}` 主页发布
  「收藏的文章」标签页，由公开的 `GET /api/readers/{id}/bookmarks` 提供数据，
  任何人无需登录即可浏览，新保存的在前。隐私姿势与 round 360 完全一致：标志默认
  关闭；从未开启的读者没有该标签页；对于「读者不存在」与「读者未公开」两种情形，
  接口一律返回一致的 404（无从猜测是否存在 / TA 收藏了什么）；公开列表只含
  对公可见的文章（不泄露草稿/定时文章）。开关随 `/account` PATCH、`/me` 与公开
  主页信封、数据导出一起传输；新增 `public_bookmarks` 列（迁移 i2k4m6n8p0r2，
  默认值沿用 round 361 修正过的 `sa.false()` 方言安全写法），按读者维度
  no-store 提供。
- **文章页「猜你喜欢」（round 362）**：首页自 round 278（DEC-128）起就有按读者亲和度
  打分的「猜你喜欢」栏，但文章页——「接下来读什么？」意图最强的时刻——过去只有
  话题相似的相关文章和「作者的其他文章」，都没有针对当前读者真正的口味。现在已登录
  读者读完一篇文章后，文章末尾会出现一条个性化推荐，按 TA 自己的阅读历史/收藏亲和度
  打分（复用了首页同一个 `/api/reader/me/recommendations` 调用）。访客与未登录读者
  什么都看不到；没有任何亲和度的新读者也不会看到孤零零的标题栏——只有确实有内容可
  推荐时才显示该栏。
- **收藏「待读 / 已读」队列（round 361）**：过去「已保存的文章」是单一无差别的概念——
  收藏既代表「等会儿再读」也代表「已经读完想留着」，混在同一个列表里无法区分。
  `/bookmarks` 页面现在把二者分开：新增「全部 / 待读 / 已读」筛选行（带实时计数，
  与文件夹、搜索筛选叠加），每行一个切换按钮可以把已保存的文章标记为已读（从待读
  队列中移除）或移回待读，已读的行带有绿色「已读」徽标。「已读」状态是读者自有字段：
  新增 `reader_bookmarks.done` 列（迁移 g1h3i5k7m9n1，默认关闭，让每个既有收藏都
  读作待读）、`GET /api/reader/me/bookmarks?done=…` 筛选、幂等的
  `PATCH /api/reader/me/bookmarks/{id}/done`、列表序列化与可移植数据导出（DEC-334）
  都带上 `done`，并支持已登录读者的云端同步——切换立即 PATCH 上传，合并时保留
  登出状态下标记的已读状态（PUT 推送无法携带 done，刚推送到服务器的行不能把它
  覆盖回待读）。顺带修复了迁移审查发现的 SQLite 方言缺陷：`done`/`public_likes`
  改用 `sa.false()` 作为默认值，保证既有行在两套引擎上都读回 `false`（字符串形式会
  在 SQLite 里存成 TEXT `'false'`，被 bool() 读成 True）。
- **公开「喜欢的文章」主页标签（round 360）**：round 359 把读者的点赞变成了持久的云端
  记录，但唯一的展示面是读者自己私有的 `/liked` 页面——点赞对其他人完全不可见。现在
  读者可以在 `/account`（简介/头像旁的一个复选框，默认关闭）选择公开，在公开的
  `/readers/{id}` 主页发布「喜欢的文章」标签页，由公开的 `GET /api/readers/{id}/likes`
  提供数据，任何人都能浏览——这是第一个读者对读者的发现维度。隐私姿态不变：默认关闭；
  从未开启的读者没有任何标签页；公开接口对未知读者和未公开读者同样返回 404（同一个
  无法区分的答复——既不是「该读者是否存在」也不是「TA 喜欢什么」的预言机）。该标志一并
  汇入 `/account` PATCH、`/me` 与公开主页信封、以及可移植数据导出（点赞与标志都加入
  导出包，DEC-334）。新增 `public_likes` 列（迁移 f6h8j0l2n4p6），不涉及缓存
  （按读者维度的数据，直接不缓存）。
- **云端同步的点赞与喜欢列表（round 359）**：点赞过去只是一个 localStorage 标记，
  读者只能单向添加——一次性、仅限本机、没有列表、也无法撤销。现在已登录读者的点赞
  是持久的跨设备云端记录：文章页的爱心按钮变成真正的开关注（再点一次即取消点赞并
  递减计数——服务器只在真正删除时才递减），登录前产生的点赞会在登录时提升为云端记录
  （与收藏文档里相同的「本地新增优先」合并策略），并新增带登录鉴权的 `/liked` 页面——
  这个「我欣赏的文章」视图与收藏（想读）和阅读历史（读过）并列——挂载时把服务器端喜欢
  集合合并下来，最新点赞在前、分页展示，在一台设备上点的赞会在另一台设备上出现。
  未登录访客保留匿名的一次性去重点赞；会话过期时在 `/liked` 上显示重新登录提示，
  而不是悄悄丢失标记。
- **作者头像（round 358）**：「人」形作者面已经有了声音（作者简介，round 357），
  却没有面孔——所有渲染都只是一个通用用户图标，而读者早在 round 299 就有了头像。
  现在超级管理员可以在用户管理里（笔名旁）上传一张小型头像（与读者头像相同的
  校验与压缩管线，以及独立的 `static/avatars` 存储），这张脸会出现在作者出现的
  每一处公开位置：文章卡片署名、文章页署名、「该作者的其他文章」模块、`/authors`
  索引卡片以及归档页头部。无主语安全姿态不变：头像仅是限定文件名的 `/static`
  路径，未设笔名的管理员依旧返回 404，无任何可泄露内容。
- **作者简介（round 357）**：round 343-356 打造的「人」形作者面——署名、归档、索引、
  关注——始终没有介绍这位作者本身。现在超级管理员可以在管理后台的用户管理里（笔名旁）
  设置一段简短的公开「关于这位作者」文字（与 round 352 的读者简介相同，纯文本 500 字上限），
  它会渲染在作者 `/authors/{id}` 归档页名称下方——这正是读者决定是否关注他的页面——并在
  `/authors` 索引卡片上显示一行摘要。归档信封（新的 `AuthorArchive` 结构）承载该文字，
  而每篇文章的署名仍然保持精简，任何列表响应都不会在每个卡片上重复这段文字。无主语的
  安全姿态不变：未设笔名的管理员依旧返回 404，无简介可泄露。
- **该作者的更多文章（round 356）**：读者刚读完并喜欢上一篇文章时，过去没有任何就地浏览这位
  作者其他作品的入口——相关文章模块是按主题的（分类 / 标签），归档页则要离开当前文章。现在凡
  是带笔名作者的文章，结尾都会出现「{作者} 的更多文章」模块：该作者最近的其他文章最多 4 篇
  （最新在前，自动排除当前正在读的那篇，绝不自我回链），并附「全部文章」入口直达归档页。它
  直接复用现有的公开作者归档接口（后端零改动）——把 round 353-355 尚未覆盖的「人」形发现
  展面，落到读者正沉浸于作品的当下。
- **从作者归档页关注作者（round 355）**：作者关注（round 353）只存在于文章署名与 `/account`
  里，却不在读者真正决定「要不要关注这位作者」的页面——公开的 `/authors/{id}` 归档页。它的
  顶部本就有 RSS 订阅链接；现在已登录读者可以在旁边一键完成站内关注（新文章收件箱 + Web Push +
  进入 `/follows` 关注流），并沿用署名按钮的全部语义（访客隐藏 / 会话过期 / 失败提示）。复用
  该组件还补上了一处真正的死胡同：尚未发布任何文章的作者没有任何署名按钮，归档页是他唯一可能
  存在被关注入口的地方。
- **关注流纳入作者（round 354）**：round 353 让读者可以关注某位作者，但这条关注没有任何浏览
  展面——作者的新文章只出现在混杂的通知收件箱里。如今作者关注与主题关注一样获得探索回报：
  `/follows` 关注流与首页「关注内容的最新文章」一行（两者共用同一接口）都会包含所关注作者
  的全部近期文章，与分类/系列/标签命中去重、一如既往按最新在前排序；带作者署名的卡片会显示
  笔名（`lucide:user` 徽标），一眼就能看出哪些文章是为「人」而来、哪些是为「话题」而来。
- **关注作者（round 353）**：在多人协作的博客上，读者会因为喜欢某位作者的文章而只想关注这
  个人——按主题的分类/系列/标签关注无法表达这种诉求。现在每篇文章的署名旁都有关注按钮（仅
  登录读者可见）：点一下即订阅该作者的新文章；`/account` 的「关注的作者」列表为每位作者提供
  独立的通知开关与取消关注；该作者每发布一篇新文章，都会以站内通知记录 + Web Push 推送到关
  注者的收件箱——与主题关注相同的推送形态，但聚焦到一个人。取消关注即刻停止推送；只有设了
  笔名的作者才可被关注——仅有登录名的管理员与未知 id 一样返回 404（与全站一致的"无预言"
  姿态）。
- **读者个人简介（round 352）**：一个读者社区的价值取决于其中的人，而只有名字、头像和加入
  时间的个人主页无话可说。读者如今可在 `/account` 写一小段「关于我」——纯文本、一小段、
  上限 500 字——它会在公开的 `/readers/{id}` 主页上显示在显示名之下（与其已通过评论和连续
  阅读记录并列），让评论者真正成为一个可以被认识的、有血有肉的人。
- **单篇文章评论开关（round 351）**：博客会存活很久，而一篇文章可能会引来互联网上的各种
  评论——高噪音讨论串、过时的常青内容、涉及隐私的文章——如今站长可以只关掉这一篇的评论，
  既不必删掉对话，也不必全局禁用评论。文章编辑器里新增「评论已开放 / 评论已关闭」开关，
  对应 `posts.comments_enabled`；公开文章页保留原有评论串，但用「这篇文章的评论已关闭。」
  提示替换评论表单，API 也会明确以 403 拒绝新评论（无论开闭，已有评论始终可读）。
- **别名变更跳转（round 350）**：当管理员为文章、系列或静态页面重设别名时，所有已散布在外的
  旧链接——分享链接、搜索结果、RSS 订阅、站内收藏——过去都会落到 404。重设别名现在会记录一条
  永久跳转：旧 URL 在 API 层仍返回 404（公开面保持"无预言"姿态），但携带 `X-Redirect-To`
  头标注规范目标，公开站点把它转化为真正的 HTTP 301——爬虫把链接权重转移到新地址而不是浪费掉，
  分享链接与收藏直达新别名；反复改名也不会形成跳转链：A→B 再 B→C，A→C 一步到位。
- **后台「复制文章」（round 349）**：编辑重复形状的内容（周报、发布说明、系列章节模板）
  时，每做一篇相似文章都要手动拷贝上一篇。如今文章列表中新增「复制」操作，可把一篇文章
  克隆成新草稿——正文、摘要、封面、分类、标签、系列归属与作者署名原样保留，生成新的唯一
  别名 `{slug}-copy[-N]`，并清空全部发布元数据（未发布、未定时、未置顶、阅读/点赞归零），
  因此这份副本是绝不会外泄或自行通告的私有草稿——随后编辑器直接跳入新草稿。评论与通知
  不会被复制。
- **站点地图覆盖页面与作者（round 348）**：站点地图随站点一同成长——`/pages/{slug}`
  （round 347）与设了笔名的 `/authors/{id}` 归档 + `/authors` 索引（round 343/346）都是
  真实、可被索引的页面，但缓存的 `/sitemap.xml` 仍只列出文章、系列、分类与标签，导致
  一篇刚发布的隐私政策或作者归档页虽然可经链接访问，对搜索引擎却不可见。如今站点地图会
  输出每一篇已发布页面（带 `lastmod`；草稿不入图）与每一位已设笔名作者的归档（未设笔名
  的管理员依旧不出现，维持无名册可探测）；任何页面的创建/更新/删除都会使 feed 缓存失效，
  因此页面一经发布，下一次渲染便进入站点地图，而无需等待缓存过期。
- **静态页面 CMS（round 347）**：自托管博客应当能为自己发声——隐私政策、条款、联系、
  变更日志——但唯一的静态页面却是一个写死的 `/about`，站点所有者每新增一个这样的页面
  都要改代码并重新部署。如今管理员可以在专门的 `/admin/pages` 管理器中创建、发布并管理
  经过整理标注的 Markdown 页面（标题 + 支持中文的自动别名 + Markdown 正文 + 发布开关，
  行内编辑、发布/下架、确认后删除），并以与文章相同的 Markdown 渲染管线发布到
  `/pages/{slug}`（代码、数学、mermaid、图片均可渲染）。公开面无名册可探测——未发布或
  未知的别名返回与不存在相同的 404，无法枚举草稿——已发布页面还会出现在站点页脚，因此
  隐私政策或条款页面无需改代码部署即可被发现。
- **公开作者索引 (DEC-359, round 346)**：`/authors` 此前是单一页面的死胡同——署名或归档链接
  只能带你到某一位作者，却没有一个地方列出所有已设笔名的作者；多编辑博客上的作者发现（以及
  曾有去处的顶层「作者」导航项）只能靠记忆。如今 `/authors` 成为索引页：每位已设笔名的作者
  一张卡片（带已发布文章数），每张卡片链接到该作者的归档页——由新的匿名、可缓存的
  `GET /api/authors` 支撑，该接口只列出已设置公开笔名的作者（未设笔名的管理员保持不可见，
  维持无名册可枚举的姿态）；归档页上也新增「全部作者」返回链接。
- **作者专属 RSS 订阅 (DEC-359, round 345)**：RSS 读者如今可以单独关注某一位作者——`/authors/{id}`
  会输出 `<link rel="alternate">` 自动发现与一个可见的订阅链接，指向 `/rss/authors/{id}.xml`，该源
  只包含此笔名作者已发布的文章，并按与分类/系列订阅源相同的契约提供服务。未知 id 与未设笔名的
  管理员返回与归档页相同的 404，因此该源无法枚举管理员。
- **后台文章编辑器作者指定 (DEC-359/TASK-406)**：文章编辑器新增作者选择器——任何
  管理员都可以把文章署给另一位已设笔名的作者（或保持默认「写作管理员」）；编辑器会预选
  文章已存的作者，公开署名与作者归档页随被指定的作者展示。新增专门的 `/api/admin/authors`
  接口（仅需管理员登录，刻意不设为超级管理员专属）只暴露笔名，因此编辑可以指派作者而不必
  触碰到与凭据相邻的 `/api/admin/users`。
- **文章作者归属 (DEC-359)**：多编辑博客一直展示文章、分类、系列、标签，却从来不
  显示「谁写的」：文章没有任何作者标识，没有署名，也无法浏览某一位作者的已发布文章。
  现在文章带有作者（创建时默认为当前写作管理员）；管理员可设置公开笔名——刻意与登录
  用户名区分，用户名始终保密（管理员登录不提供名册，暴露用户名等于交出凭据的前半段）
  ——凡是设置了笔名的作者，其每篇已发布文章都会在文章页和列表卡片上显示署名，并链接到
  `/authors/{id}`：一个按笔名组织的分页归档页，仅收录该作者已发布的文章，即使作者尚未
  发布任何内容，页面标题也会按笔名显示。未知 id 与未设笔名的管理员返回同样的 404，
  因此该入口无法枚举管理员。
- **读者修改登录邮箱 (DEC-357)**：登录邮箱原是写死的，没有任何验证流程。读者地址变更
  后就只能被「困死」—— 逐事件邮件与每周摘要继续发往旧邮箱，唯一的「补救」是删除账号
  （数据全部丢失）。现在已登录读者可以在账号设置中更换登录邮箱：填写新邮箱与当前密码后，
  一封一次性验证邮件会发往**新**邮箱（60 分钟内有效，可重复发起 —— 新请求会替换待处理
  的变更），点击链接即可完成切换：邮箱替换、token_version 递增（吊销此前所有会话）并自动
  用新邮箱登录。点击前旧邮箱仍可正常登录；等待期间目标地址若被其他账号占用则返回 409；
  任何接口都不会泄露某地址是否属于某个账号。
- **订阅者每周摘要 (DEC-355)**：访客邮箱订阅（DEC-351）只有一种节奏 —— 每篇新文章
  一封邮件。博客发文频繁时，订阅者会每天收到一封邮件，却没有任何「更轻」的选择
  （读者账号早已同时拥有逐事件邮件与每周精选，DEC-201/DEC-326）。现在访客订阅者
  也可以选择每周摘要：在页脚表单勾选，或确认页切换即可。每周摘要任务 —— 与服务读者
  的是同一个任务（同一个 advisory lock、单条 SMTP 会话、同样的滚动 7 天窗口、送达
  后才幂等打点）—— 每周发送一封按站点语言渲染的汇总邮件，内含文章深链与订阅者自己的
  token 退订页脚。选择每周摘要的订阅者不再收到逐篇邮件，正好收到自己选的节奏，绝不
  重复。逐篇仍是默认，摘要与读者 schema 一样为显式opt-in。
- **管理员订阅者管理 (DEC-354)**：访客邮箱订阅（DEC-351）此前没有运营端入口 —
  管理员无法查看谁在订阅列表里、多少已确认/待确认，也无法移除某个地址。现在管理后台
  提供订阅者列表（邮箱、已确认/待确认标签、订阅日期），支持状态筛选、不区分大小写的
  邮箱搜索、分页，以及每行一个移除操作（彻底删除该行与 token —— 之后该 token 的退订
  请求一律 404，与「从未订阅过」无法区分，不构成探测面）。没有管理环路的订阅功能无法
  处理被他人代订的地址、失效的邮箱、或丢失 token 而要求移除的订阅者 —— 这个页面补上
  了这条闭环。
- **游客邮箱订阅（Newsletter）(DEC-351)**：博客的所有邮件能力（周报、逐事件邮件、
  访客回复邮件）此前都要求先注册读者账号或留评论 — 只想"有新文章邮件通知我"的匿名访客，
  除了 RSS（技术读者的工具）没有任何入口。页脚表单正是这个入口：任何访客输入邮箱地址，
  收到一封双重确认链接（未点击前不会收到任何邮件），确认后每发布一篇新文章就会收到一封邮件 —
  深链到文章、自带每条订阅专属的退订链接、按站点配置语言发送。订阅接口永远返回同一条通用消息
  （不构成邮箱存在性探测，与读者注册一致）；确认/退订幂等，未知 token 一律 404（与回复提醒
  退订一致）；定时发布到点经 fire-on-read sweep 触发同一封恰一次邮件；邮件发送失败绝不阻断
  触发它的发布（best effort，未配 SMTP 时静默关闭）。
- **首页的服务端"继续阅读"行 (DEC-348)**：首页的"继续阅读"行此前是纯设备本地
  localStorage 记录 — 已登录读者换到新设备会看到空行，即使服务端阅读记录里存着他上次读到
  哪里。如今该行改为从服务端取已登录读者「存有续读位置」的文章（最新在前），点击即从保存的
  位置继续 — 续读真正跨设备，和服务端续读数据同处一处。服务端记录优先于本设备本地记录；
  访客继续使用轻量的本地记录不变。
- **跨设备续读位置 (DEC-346)**：续读位置此前是相对某一台设备布局的绝对像素偏移 — 读者在
  桌面上读到 900px 离开，换到手机上"900px"就是另一个位置。现在文章页还会把位置存成可滚动
  文档高度的 0..1 比例（`scroll_fraction`，与 `scroll_position` 并存），恢复时优先使用
  fraction 并按当前视口换算 — 手机↔桌面（或任意尺寸）之间继续阅读都会落在同一处。功能之前的行
  仍走像素回退，回到顶部的清除会同时抹掉两个值。
- **每个公开展面的定时文章通告 (DEC-344)**：定时文章上线时的 fire-on-read 通告
  （DEC-336）此前覆盖文章列表、详情与站点级订阅，但其它会展示刚上线文章的展面
  （`/follows`——关注者自己的聚合流；搜索；分类/系列订阅；sitemap）不会触发它——只看
  `/follows` 的关注者会看到文章上线却没有收到任何推送/收件箱/邮件。如今每个这样的展面都
  触发同一条恰好一次的 fan-out，关注者无论在哪个页面都能收到通告。
- **站点语言的访客与找回邮件 (DEC-342)**：读者侧的邮件已按读者本地化
  （DEC-338/DEC-340），但发送者侧的邮件仍是硬编码中文——访客回复邮件（没有账号的访客收到的
  「有人回复了你的评论」）和密码重置邮件（双语 subject 加全中文正文，且把站点名硬编码成
  "X-Blog" 而非配置的站点名）。现在两者都按站点配置的语言（`SITE_LANGUAGE`，默认 zh 保持
  原有文案）渲染，重置邮件使用配置的 `SITE_TITLE`——配置为英文的站点如今所有访客与找回邮件
  都是英文且站点名正确。
- **读者语言的回复/讨论/@提及文案 (DEC-340)**：读者语言的故事（DEC-338）此前只覆盖新文章通知
  ——英文读者会收到英文的 "New post" 邮件，但"有人回复你 / 订阅的讨论有新评论 / 被 @提及"的
  收件箱行与邮件仍是硬编码中文。现在其余类型也按读者语言本地化：英文读者切换后，收件箱行与
  邮件会得到 "Someone replied to your comment"、"New comment in a thread you follow"、
  "You were mentioned in a comment"（带评注者昵称），与同一批里的中文读者保留
  「有人回复了你的评论 / 你订阅的讨论有新评论 / 有人在评论中提到了你」并存。浏览器推送载荷仍
  为运维级的全局文案（单个载荷无法按读者浏览器区分语言）。
- **读者语言的通知文案 (DEC-338)**：通知文案此前对所有读者都硬编码为中文——持久收件箱标题
  （系列更新 / 新文章发布 / 《…》）、按事件发送的邮件主题与正文、以及每周精选邮件全部是中文，
  与读者的界面语言或站点语言无关。现在登录读者在语言切换器里切换语言时会持久化到账号
  （新增 `PUT /api/reader/me/locale`），分发时按每位读者的语言渲染文案：英文读者从切换那一刻起，
  收件箱行、邮件与精选摘要都是英文（"New post" / "Series update"），而从未切换（或存量）读者
  保持原有中文文案不变——locale 列为纯增量，NULL 即按站点默认（zh）处理。
- **定时文章上线时通知关注者 (DEC-336)**：以"已发布但 publish_at 在未来"方式创作的帖子是
  一等公民的编辑日历能力，但新文章分发只在 WRITE 时刻、文章已经可见时才触发——因此当
  时钟跨过 publish_at、文章公开的那一刻，没有任何关注者收到 push、持久收件箱行或邮件
  （本博客没有后台调度器去感知这个跨越）。新增的 fire-on-read 扫描
  （`crud.maybe_notify_due_scheduled_posts`）会在第一个公开读到它的时刻（文章列表、
  详情、RSS/Atom 订阅——外部订阅轮询是最可靠的触发源）一次性通知所有"已跨越但从未被通告"
  的文章；同一枚持久 `new_post_notified_at` 戳也被写入时路径（创建/更新/还原/后台编辑器）
  打上，因此一篇文章绝不会被重复通知。即使在并发 worker 下也保证恰好一次，并且是
  追溯式的：本特性上线前就已跨越的定时文章会在下一次公开读取时被通知。
- **完整的读者数据导出 (DEC-334)**：`GET /api/reader/me/export` 是面向 GDPR 的"下载我的
  数据"导出包，但此前只包含账号/收藏/评论/历史——读者无法找回自己关注的分类/标签/系列、
  通知偏好、持久收件箱记录或设备注册信息。现在导出包新增 `follows`（分类/标签/系列及其
  notify 开关）、`notification_prefs`、`notifications`（持久收件箱行）以及
  `push_subscriptions`（endpoint + created_at，绝不包含加密密钥）。跨读者数据与
  草稿/定时文章泄漏不变式保持一致；契约测试覆盖一个"每样都有"的读者走真实 API 导出。
- **访客评论者回复邮件 (DEC-332)**：评论表单要求匿名评论者必须留下一个邮箱，该邮箱
  一直保存在评论行上却从未被使用——回复通知的判断是 `parent.reader_id is not None`，
  因此对匿名评论的回复在任何一个通道上都通知不到任何人。访客现在勾选新增的"有人
  回复我的评论时邮件通知我"（默认关闭）后，当他们的评论收到一条被审核通过的回复
  时会收到一封邮件，深链到那条回复，并附带一个按评论粒度生成的退订链接；从未勾选
  （或之后退订）的访客绝不会被邮件打扰，且 SMTP 未配置时审核仍然成功（尽力而为，
  与其它所有通知通道一致）。纯增量改动 —— 在 `comments` 上新增 `reply_notify_email`
  与 `reply_notify_token` 列（DEC-009）—— 并新增公开接口
  `POST /api/comments/reply-notify/unsubscribe`（幂等；未知 token 返回 404）以及
  `/comment-reply-unsubscribe?token=` 落地页。配套后端契约测试（勾选后审核回复即发
  邮件并携带深链与 token；未勾选 / 无邮箱 / 读者父评论走读者分发而不发访客邮件；
  自回复跳过；退订后不再发送；未知 token 404；SMTP 未配置无操作；token 与同意字段
  绝不进入公开评论列表）、CommentForm 勾选框单元测试、退订页面测试，以及一条经由
  SMTP sink 的端到端旅程。
- **@提及的邮件副本 (DEC-326)**：@提及分发（DEC-322）此前只落在持久收件箱和浏览器
  推送，因此依赖邮件通道（DEC-197）的读者永远收不到最私人化的通知——被别人点名。
  提及分发现在会在读者开启新的按类型偏好 `email_mention`（与其他邮件类型一致，
  默认关闭）时同时发送邮件，且与应用内提及开关相互独立（读者可关闭收件箱行但仍
  接收邮件，反之亦然）；邮件携带与收件箱行完全相同的标题/正文/深链。在
  `reader_notification_prefs` 上新增纯增量 `email_mention` 列（DEC-009）。配套后端
  契约测试（默认关闭但收件箱行仍落地；开启后发送到注册邮箱；应用内提及关闭后邮件
  仍发送；偏好开关往返；偏好面暴露新类型）+ 一个针对新偏好行的前端页面测试。
- **@提及自动补全 (DEC-324)**：审核通过后的评论可以 @ 提到某位读者（DEC-322），
  但评论者必须凭记忆打出对方的完整显示名——没有任何发现途径，这个能力在实操中
  几乎不可用。在评论框输入 `@` 现在会弹出匹配读者的候选列表（前缀匹配优先、其次
  子串，最多 8 条），由新增的公开接口 `GET /api/readers/suggest` 提供（只返回 id +
  显示名 + 头像，绝不返回邮箱）。支持上下方向键 / Enter / Escape / 鼠标点选；
  选中后会在光标处插入 `@显示名 `，让提交后的评论真正提到对方。配套后端契约测试
  （子串 + 前缀排序、LIKE 通配符转义、停用/无显示名读者排除、结果有界、不含
  邮箱/PII）、CommentForm 选择器单元测试（打开/移动/插入/取消/无结果），以及一个
  e2e（输入 `@pick` → 选择器 → 点选 → 提交 → 审核通过 → 被提及读者的收件箱出现
  mention 行）。无 DDL。
- **@提及通知 (DEC-322)**：当一条已审核通过的评论提到某位读者的显示名（如
  `@Riki`）时，该读者现在会在收件箱收到一条通知，并深链到那条评论。此前没有任何
  方式能把某个人叫进讨论——只能回复评论，或寄望对方关注了线程/分类。触发时机在
  审核通过时（待审核评论不通知任何人），会跳过评论者本人，尊重读者按类型进行的
  逐项关闭，并按词边界匹配（`@Riki` 永远不会通知名叫 `Ri` 的读者）。新类型为
  `mention`：与其他应用内类型的默认开启保持一致（与 DEC-171 一致），由
  `reader_notification_prefs` 上的一个**纯新增** `mention` 列承载（DEC-009 保持
  DDL 不可破坏；`ReaderNotification.kind` 为 `String`，新类型无需对通知表做 DDL）。
  配套后端契约测试（派发、自我提及跳过、关闭偏好、停用读者、边界匹配、批量扫描）、
  前端页面测试（类型标签 + 偏好开关），以及一个 e2e 旅程（游客评论 `@<名字>` →
  管理员审核通过 → 被提及读者收件箱出现 `mention` 行且 `#comment-<id>` 链接可落地）。
- **评论历史深链 (DEC-321)**：`/comments` 页面与读者公开主页此前把每条评论链接回
  其文章的**标题处**，读者想找自己的（或主页上的）某条评论时只能落在线程顶部再手动
  翻找。"在文章上"的跳转现在携带 `#comment-<id>` 锚点——文章页评论列表本已具备的
  定位机制（回复通知深链 DEC-072 已验证）会直接把读者带到那条评论。纯前端改动：
  两处 `NuxtLink` 目标（`app/pages/comments.vue`、`app/pages/readers/[id].vue`）；
  附页面单元测试与一个 e2e 旅程（评论 → 审核通过 → `/comments` → 点击 → 落在
  `#comment-<id>`）。
- **站点地图完整性：系列 + 全部文章，无静默上限 (DEC-318)**：站点地图此前完全遗漏
  系列——`/series/[slug]` 页面可被索引却无法经站点地图触达——并且以硬编码
  `limit=1000`、无分页地抓取文章，一旦博客超过 1000 篇已发布文章，更早的文章就会
  静默地从站点地图（进而从搜索引擎）消失。`sitemap.xml` 现在逐页遍历全部已发布
  文章（每页一次抓取，内存有界，无上限），并为每个系列输出一条 `/series/{slug}`
  记录；新建系列也会立即失效已渲染的站点地图缓存，而不再等待 TTL。附后端契约
  测试（用分页桩验证分页循环；系列条目）与一个在 Nuxt 源站创建系列后抓取站点
  地图的 e2e。无 DDL。
- **读者本地的阅读连续与热力图 (DEC-316)**：`/history` 的阅读连续与 52 周活跃热力图
  过去一律按 UTC 分桶、却以浏览器本地时间渲染——任何非 UTC 时区的读者都会看到连续
  天数记错日历日、热力图提示气泡/"今天"列与自己的日历不符（晚上的阅读甚至会被记到
  下一个 UTC 日，提示气泡还会显示提前一个本地日）。统计接口现在随请求携带浏览器的
  IANA 时区，后端按读者的本地日历日对阅读分桶（连续阅读与热力图窗口同样锚定本地
  今天）；热力图标签随后按已是本地的日期直接展示，不再按浏览器偏移二次位移。附后端
  契约测试（跨日界线分桶偏移、未知时区 422、本地今天锚定）、前端测试，以及一个
  固定在 UTC−1 时区、强制本地日期与 UTC 不同的 Playwright 旅程——今天单元格的提示
  必须显示本地日期、绝不显示纯 UTC 标签；回退成纯 UTC 分桶的实现会被确定性判负。
  无 DDL；后端镜像现在显式安装 `tzdata`，保证生产环境 IANA 时区可解析（否则页面总是
  声明时区后，缺失的时区数据会让每一条统计请求都被拒绝）。
- **标签范围 RSS 经 Nuxt 代理正常生效 (DEC-314)**：标签页把订阅源定位为
  `/rss/feed.xml?tag_id={id}`（自动发现 + 订阅按钮），但 Nuxt 源站的订阅源代理把
  查询字符串剥掉了——在默认 compose/`nuxt preview` 栈（前置无 nginx）下，订阅单个
  标签的读者会静默收到全站订阅源而不报任何错误。兄弟范围订阅源（分类/系列，路径
  形式）一直正确代理，标签源于是成为唯一失效的范围。`proxyConditionalFeed` 现在
  把入站查询原样转发给后端（与 API 代理一致），使标签范围订阅保持限定；后端的
  按内容 ETag 让 304 重新验证在跨范围时依然正确。附服务端单元测试（代理 URL 带
  `tag_id`、无查询时保持无查询的全局 URL）与一个经 Nuxt 源站（浏览器实际访问的
  路径）抓取标签范围订阅源、验证只含该标签文章的 Playwright e2e。
- **删除单条通知 (DEC-312)**：读者通知收件箱（DEC-160）是持久化的，但此前没有
  清理途径——标为已读/全部已读只清除角标，订阅多个系列/分类的读者的已消费通知
  会无限累积。读者现在可以在 `/notifications` 删除任意一条通知（每行"标为已读"
  旁的垃圾桶按钮），该行会即时从列表移除；若删除的是未读条目标记，未读数同步
  下降。新增 `DELETE /api/reader/me/notifications/{id}`（与所有读者表一样按
  reader_id 限定：未知或他人 id 返回 404，绝不越权删除）。附后端契约测试
  （删除、隔离、404、超大 id 422）、前端页面测试（行移除、失败保留行、角标保持
  真实）与一个 Playwright e2e 旅程（种子两行、API 删一行、UI 删剩余至空态）。
- **待审核评论的定位修复 (DEC-310)**：在审核制部署（默认关闭自动通过）下，创建
  接口返回的是待审核评论，而列表只返回已通过的评论——此前提交后，线程定位逻辑
  会翻页并逐页查找一条结构性不可能渲染的评论，表现为静默滚动乱跑、看似提交失败
  并诱导重复提交。评论列表现在依据创建响应的 `is_approved === false` 完全跳过
  跳页/翻页查找，把"等待审核"的明确提示当作唯一真相；已通过评论（自动通过读者
  层级）仍会正常定位并滚动。纯前端改动；待审核评论的审核结果会在读者重新访问
  文章页时显现。附单元测试（待审核跳过 fetch/滚动、已通过仍正常定位）与一个
  断言提交后无分页请求的 e2e。
- **评论图片灯箱 (DEC-308)**：点击评论内的图片会打开与文章图片相同的全屏查看器
  （DEC-302）——原始分辨率、ESC/背景/关闭按钮均可关闭、方向键浏览该评论（含
  其嵌套回复）的图片、关闭后焦点归还触发元素。此前评论正文把 markdown 图片渲染
  成无法细看的普通通栏 `<img>`（与文章同一条 marked+净化管线，却没有查看器）；
  粘贴截图/示意图的评论者无从放大。评论列表上做点击委托，只通过同一
  `sanitizeUrl` 安全过滤收集可加载的 http(s)/相对 src，`javascript:`/`data:`
  src 永远打不开查看器。新增单元测试（点击打开、危险 src 排除）与
  `comment-lightbox.spec.ts` Playwright e2e 旅程。
- **评论实时预览 (DEC-306)**：评论表单标注了净化后的 Markdown 渲染（DEC-088），
  且每条评论都要进审核队列（DEC-066），但评论者提交前完全看不到草稿的渲染效果——
  一旦标记写错，就会白白占用一次审核且毫无反馈。表单现在带有一个"撰写/预览"
  切换：预览页签通过评论列表同一条 `commentMarkdownToHtml` 管线渲染草稿（同一
  净化器、同一惰性 highlight.js 代码高亮），真正做到"所见即所发"。提交成功后
  自动回到清空的撰写页签；草稿为空时显示提示而非空白面板。纯前端净增量切片
  （无后端/DDL），附单元测试（渲染、XSS 惰性预览、切换保留草稿、提交复位）与
  Playwright e2e（Markdown 渲染 + script/事件处理器载荷绝不进入 DOM）及中英文案。
- **Markdown 图片灯箱 (DEC-302)**：点击文章内任意图片会在深色全屏查看器中以原始
  分辨率显示——按 Escape、点击背景或关闭按钮即可关闭；方向键（或屏幕上的箭头）
  在文章图片间循环浏览，并显示 "n / total" 计数；关闭后键盘焦点回到打开查看器的
  图片。Markdown 图片语法（`![alt](src)`，即文章的实际撰写方式）现在会进入与
  HTML `<img>` 标签相同的懒加载分段管线（此前它会绕过分段器、以普通行内 `<img>`
  渲染，既没有懒加载也永远进不了查看器）；`javascript:`/`data:` 图片 src 会被净化
  出查看器集合，永远不会打开。多年前因"承诺了并不存在的查看器"而移除的
  `cursor-zoom-in` 光标提示，如今名副其实。新增 `MarkdownLightbox.vue`（Teleport
  遮罩，带焦点管理与 body 滚动锁定），接入 `MarkdownContent.vue`，并配有组件、
  composable、Playwright e2e 测试与中英 i18n。
- **读者头像 (DEC-299)**：读者现在可以在 `/account` 设置个人头像（上传或移除），
  补全了读者主页（DEC-294）开启的身份表面。头像会渲染在公开 `/readers/{id}`
  主页上，以及所发评论旁已认证读者名字的边上；未设置头像的读者仍显示首字母占位。
  新增 `POST /api/reader/me/avatar`（multipart，复用媒体上传的打点式图片校验——
  类型白名单、大小上限、魔术字节、完整 Pillow 解码、绝不增大的再编码）与
  `DELETE /api/reader/me/avatar` 接口，新增兼容性的 `reader_accounts.avatar_url`
  列（DDL 保持，DEC-009），并让 avatar_url 贯通读者资料、公开主页与评论读者
  结构。后端契约测试 + 前端页面/组件测试 + Playwright e2e + 中英文案。
- **读者隐私修复 (TASK-377)**：从未设置显示名的已登录读者，此前会以其账号邮箱
  作为评论昵称显示在公开评论区。现在这类评论统一显示通用的"读者"身份——邮箱绝
  不会作为公开名字出现在文章评论区（历史评论同样生效），已认证读者徽标保留。
- **读者公开主页 (DEC-294)**：评论者的认证显示名现在是一个可点击的身份——在文章
  里，已登录读者的一句已通过评论，其名字会链接到 `/readers/{id}` 这一公开（无需
  登录）主页，展示显示名、加入时间与 ta 在公开可见文章上已通过的评论（分页）。
  匿名评论者仍显示手填昵称；未知读者 id 渲染"读者不存在"而不是空页面。评论列表
  的链接取决于读者身份（而非昵称——后端会把昵称覆盖为显示名），因此每位认证评论者
  都会渲染出链接。新增 `GET /api/readers/{id}` 路由（主页 + 评论列表，不含邮箱/
  最后登录等隐私字段）+ 后端测试、页面与组件测试、Playwright e2e 及中英文案。
- **关注流专页 (DEC-292)**：新增 `/follows` 页面，已登录读者可在此浏览其关注的
  分类、系列与标签下的所有新文章，并支持分页翻过首页 12 篇的封顶。关注流接口
  （`GET /api/reader/me/follows-feed`）升级为 `{items, pagination}` 分页信封，
  首页"关注内容的最新文章"区块新增"查看全部"入口跳转到该页；会话过期/失效时
  与通知收件箱一致，回到登录页。后端契约测试（含 total/total_pages 与越界页码
  回钳）+ 前端页面测试 + Playwright e2e + 中英文案。
- **系列内就地关注 (DEC-290)**：文章页的"本系列文章"导航框内新增关注控件，
  已登录读者可直接关注/取关当前系列并切换新篇通知，无需离开当前文章跳去
  `/series/[slug]` 页 —— 与文章页脚标签的就地关注（DEC-196）一致，关注持久化
  到账号页"关注的系列"。
- **阅读密度 (DEC-288)**：文章页新增 A−/A+ 字号调节（三档），并记忆在设备上，
  重新加载页面仍生效 —— 无需账号的长文阅读舒适度设置。
- **单帖阅读趋势 (DEC-287)**：后台文章编辑器显示该篇文章近 30 天的阅读量迷你
  走势图（`GET /api/admin/stats/views/posts/{id}`），作者可一眼看出文章热度
  是上升还是衰减 —— 对应后台聚合趋势（DEC-086）的单帖视图。
- **读者密码找回 (DEC-286)**：忘记密码的读者可在 `/forgot-password` 申请一次性
  邮件重置链接，并在 `/reset-password` 完成重置（设置新密码 → 撤销所有会话 →
  自动登录）。请求端点刻意不构成"账号存在性探测"，重置令牌携带独立 JWT
  audience，无法被重放为读者或管理员凭据。后端
  `POST /api/reader/password-reset/{request,confirm}` + 测试；前端
  `/forgot-password` 与 `/reset-password` 页面 + 中英文案 + 测试。
- 部署修复：前端 Docker 构建固定到 `node:24.20.0-alpine3.24` — 此前固定的
  `node:24.8.1-alpine3.20` 已从 Docker Hub 移除，导致 Deploy 工作流中
  `Build and push frontend` 失败。
- 🛠️ **SEO 加固（397 轮）**：带筛选/分页的视图将 canonical 指向真正承载内容的
  URL（`/?category_id=X` 或 `/tags?tag_id=Y&page=2` 是独立页面，绝不会塌缩到
  空壳索引页）；可分页的 feed 在多页集合上输出 `<link rel=prev|next>`；
  **`og:locale` 与默认关键词跟随界面语言**（英文读者分享预览不再声明简体中文）。
  已接入首页、归档、标签、分类、讨论、搜索、我的评论、独立页、系列、读者主页。
- 🛠️ **"控件保持挂载"的重新请求修复（397–399 轮）**：标签、分类、归档页在
  筛选值之间的 SPA 跳转时整个页面闪为骨架屏（进行中的 `pending/error` 门控会
  在交互中途卸载页头、返回链接与关注/通知控件）——云端/索引视图现在只受自身
  请求门控，筛选后的文章区只受文章请求门控；我的评论页在重新请求时也不再卸载
  状态筛选栏与关键词搜索框（不再丢失焦点）。
- ♿ **读屏器分页播报（398 轮）**：首页早已播报页面切换，但其余所有分页 feed
  （标签、分类、归档、讨论、搜索、我的评论、关注、喜欢、读者主页）对辅助技术
  而言是静默换列表。共享的 `common.state.pageAnnounce` 实时区现在统一播报落地
  页码；分页文章区保持单一 `role=status` 来源。
- 🛠️ **后端删除孤儿审计（400–402 轮）**：此前只在文章删除时清理的增量 DEC-009
  表（纯整型外键、无 ORM 级联）现在在其引用的每个删除路径上都做清理。访客
  话题订阅随文章删除一并消亡（否则被删文章可能把另一篇文章的邮件发给从未
  订阅它的关注者）；分类/标签/系列/用户删除时清理各自关注的读者行（过期的
  关注在 SQLite id 复用时可能错误指向无关的新行）。每条路径都有回归测试。

## [0.1.0] - 2026-09-09

X-Blog 的首个标记版本 — 一个基于 FastAPI + Nuxt 构建的现代化全栈博客系统。
本版本汇集了自 2026 年 4 月项目启动以来落在 `main` 分支上的全部功能
（1381 次提交；后端 1243 个测试 + 前端 1317 个测试，后端覆盖率 93.5%）。

### 新增

#### 读者体验

- **Markdown 发布**：支持 Mermaid 图表、KaTeX 公式与代码高亮（后端 `markdown` 管线 + Nuxt 渲染器）。
- **全文本站搜索**：独立搜索页、关键词高亮与摘要转义。
- **RSS 与 Atom 订阅**：站级及按分类/标签的局部订阅源，支持自动发现（DEC-074）。
- **标签与分类**：文章组织、浏览页、侧边栏标签与按关注过滤的首页信息流。
- **系列**：有序的多篇连载，系列内上一篇/下一篇导航（DEC-189）。
- **嵌套评论**：支持回复、线程化渲染与审核状态（DEC-066）。
- **读者账号**：注册/登录与读者 JWT，含账号页（邮箱、昵称、修改密码）。
- **云端书签同步**：登录后将本地书签合并到云端，并在多设备间保持同步。
- **续读定位**：文章页将已登录读者的滚动位置保存在服务端，再次访问时落回原处（DEC-167）。
- **阅读连续打卡与活动热力图**：`/history` 展示当前/最长连续阅读天数与 52 周热力图（DEC-169）。
- **读者通知收件箱**：持久化的站内已读/未读列表，覆盖关注的系列/分类新文章、
  回复与被关注讨论的新评论（DEC-160）。
- **按类型通知偏好**：可在 `/notifications` 单独关闭任意一类通知（DEC-171）。
- **标签关注**：关注某一主题标签，接收新文章推送与收件箱条目（DEC-195）。
- **邮件通知与每周摘要**：按事件发送邮件，并提供可选的每周汇总（DEC-197、DEC-201）。
- **新文章 Web Push**：分类/全部新文章的浏览器推送订阅（DEC-076）。
- **评论线程订阅**：所关注讨论有评论审核通过时推送提醒（DEC-078）。

#### 后台管理

- **管理仪表盘**：已发布/定时/草稿文章统计、待审评论队列、按浏览量统计的热门文章
  图表与站点全局统计。
- **文章编辑器**：封面图、分类/标签指派、Markdown 预览（MarkdownContent）、
  定时/草稿/发布状态、未保存修改提醒与带 401 处理的自动保存。
- **媒体库**：浏览全部已上传图片（网格、预览、复制链接、使用中标记），删除未被
  引用的上传，并在编辑器中插入（DEC-183）。
- **编辑日历**：按月网格展示发布计划，可点击跳转编辑器（DEC-162）。
- **评论审核**：待审/已通过/已拒绝管理与新评论推送提醒（DEC-080）。
- **备份与恢复**：单一可移植 JSON 快照（分类、标签、系列、文章、评论），
  可恢复到空实例（DEC-082）。
- **数据导出**：文章/评论导出为 CSV。
- **系列管理** 与分类/标签列表上的 **文章数** 展示。
- **读者管理** 与 **阅读分析**：每日读者趋势 + 热门文章（DEC-086）。

#### 平台

- **REST API**：类型化 Pydantic 模式、OpenAPI 文档与一致的数据信封
  （data + error + 分页元数据）。
- **PostgreSQL 支持** 作为生产方言（tsvector 全文搜索、咨询锁、naive-UTC 存储），
  本地开发默认 SQLite。
- **Alembic 迁移**：启动时自动执行（`alembic upgrade head`），并在 CI 中
  设有漂移检查门禁。
- **全端点限流**（读/写/登录/注册/搜索/评论/导出），支持按端点覆盖限制与
  可信代理下的客户端 IP 还原。
- **全文搜索、置顶文章、SEO**：Open Graph、JSON-LD、sitemap。
- **Docker Compose 部署**（PostgreSQL + 后端 + 前端，Nuxt 代理 + nginx，
  GHCR 镜像构建 + 经 GitHub Actions 的 SSH 部署）。
- **暗色模式**（跟随系统偏好）、**TypeScript + Pydantic 类型安全** 与
  **i18n**（中/英文，含本地化的日期显示）。

### 修复

- 导入历史在唯一键竞争条件下具备幂等性（重试一次而非 500，ISS-419）。
- 阅读历史清空不再被滞后的在途响应「复活」（ISS-425）。
- 目录锚点与标题深链正确落在对应章节；按住修饰键点击新标签页打开（ISS-422）。
- 推送订阅为单飞（single-flight）操作，不会因重复触发而误报全局「已拒绝」
  （ISS-423、ISS-442）。
- 搜索摘要/摘要在所有兜底路径上保持 XSS 安全约定（ISS-424）。
- 读者写接口纳入限流；失效会话的关注控件会清除令牌并提示登录（ISS-429、ISS-434）。
- 管理仪表盘的待审评论卡片与页头显示真实的全局队列，而非页面局部切片
  （ISS-431、ISS-439）。
- 系列单集重排为单飞操作（ISS-430）。
- 过期未读徽标轮询在更新刷新后不再覆盖结果（ISS-442）。
- 媒体删除路径的引用查找以定向探测限制范围（ISS-432），且媒体选择器在翻页重取时
  禁用并保持单飞（ISS-440）。
- 线程评论通知扇出改为批量 — 单次 flush+prune+commit，而非 O(关注者) 次事务
  （ISS-427）。
- 书签文件夹重指派失败会主动提示，而非静默丢失文件夹（ISS-428）。
- 过期的在途未读徽标轮询不再覆盖更新的刷新结果。
- CI 门禁收紧：分支覆盖率门禁真正生效（此前为隐性失效），且完整后端测试套件改为
  针对 PostgreSQL 运行。

### 安全

- 所有已认证的读者变更操作均纳入限流（ISS-429）。
- 管理员创建/更新写接口纳入限流（ISS-017）。
- API 代理请求体大小在服务端与 nginx 边缘双重限制。
- 数字型 `post_id` 限制在 64 位范围内。
- FastAPI `/docs`、`/redoc`、`/openapi.json` 不在公网边缘暴露。
- Web Push / VAPID 端点在未配置时快速失败（503）；CI 使用一次性密钥，
  确保 e2e 永不携带真实凭据。
- Sendgrid/SMTP 与 Web Push 凭据严格存放在环境变量中。

### 性能

- 媒体引用映射表缓存，并在文章写入时失效（ISS-432）。
- 线程评论扇出批量化为单事务（ISS-427）。
- 订阅源缓存按全文/摘要标记区分，避免正文冲突。
- Nuxt 静态 SSR 缓存放于 nginx（`proxy_cache`）之后。
- 针对待审数、信息流与仪表盘统计优化查询。

[0.1.0]: https://github.com/pplmx/x-blog/releases/tag/v0.1.0
