services:
  frontend:
    build:
      context: ./frontend/nuxt
      target: runner
    ports:
      - "13334:3000"
    environment:
      - NUXT_API_URL=http://backend:18888
      - NUXT_SITE_URL=http://localhost:13334
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 5s
      retries: 3

  backend:
    build:
      context: ./backend
      target: production
    ports:
      - "18888:18888"
    environment:
      - DATABASE_URL=sqlite:///./x-blog.db
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-change-me-in-production}
      - ALLOWED_ORIGINS=http://localhost:13334,http://frontend:3000
      - RATE_LIMIT_PER_MINUTE=60
      - GZIP_MINIMUM_SIZE=500
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:18888/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
